<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yunwei.mapper.OrderMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders (
            number,
            status,
            user_id,
            address_book_id,
            order_time,
            pay_method,
            pay_status,
            amount,
            remark,
            phone,
            address,
            consignee,
            delivery_status,
            delivery_time,
            tableware_number,
            tableware_status
        )
        values (
                   #{number},
                   #{status},
                   #{userId},
                   #{addressBookId},
                   #{orderTime},
                   #{payMethod},
                   #{payStatus},
                   #{amount},
                   #{remark},
                   #{phone},
                   #{address},
                   #{consignee},
                   #{deliveryStatus},
                   #{deliveryTime},
                   #{tablewareNumber},
                   #{tablewareStatus}
               )
    </insert>

    <update id="cancelTimeoutOrders">
        update orders set status = 6,cancel_time = #{cancelTime},cancel_reason = '订单超时未支付，自动取消'
        where status = 1 and pay_status = 0 and order_time &lt; #{deadline}
    </update>


    <select id="getByNumber" resultType="com.yunwei.pojo.entity.Orders">
        select *
        from orders
        where number = #{number}
    </select>

    <select id="getByNumberAndUserId" resultType="com.yunwei.pojo.entity.Orders">
        select *
        from orders
        where number = #{number}
          and user_id = #{userId}
    </select>

    <select id="listByUserId" resultType="com.yunwei.pojo.entity.Orders">
        select *
        from orders
        where user_id = #{userId}
        order by order_time desc
    </select>

    <select id="getByIdAndUserId" resultType="com.yunwei.pojo.entity.Orders">
        select *
        from orders
        where id = #{id}
          and user_id = #{userId}
    </select>

    <update id="updatePaymentStatus">
        update orders
        set status = #{status},
            pay_status = #{payStatus},
            checkout_time = #{checkoutTime}
        where id = #{id}
          and status = 1
          and pay_status = 0
    </update>
</mapper>
