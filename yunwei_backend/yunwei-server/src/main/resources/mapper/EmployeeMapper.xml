<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 必须写对应 Mapper 接口的完整包名 -->
<mapper namespace="com.yunwei.mapper.EmployeeMapper">
    <insert id="insert">
        insert into employee(
        name,
        username,
        password,
        phone,
        sex,
        id_number,
        status,
        create_time,
        update_time,
        create_user,
        update_user
        )
        values(
        #{name},
        #{username},
        #{password},
        #{phone},
        #{sex},
        #{idNumber},
        #{status},
        #{createTime},
        #{updateTime},
        #{createUser},
        #{updateUser}
        )
    </insert>
    <!-- 只更新状态和审计字段，不修改员工的其他信息 -->
    <update id="updateStatus">
        UPDATE employee
        SET status = #{status},
        update_time = #{updateTime},
        update_user = #{updateUser}
        WHERE id = #{id}
    </update>


    <!-- 根据员工 ID 删除对应记录 -->
    <delete id="deleteById">
        DELETE FROM employee
        WHERE id = #{id}
    </delete>

    <!--
        id 必须与 EmployeeMapper 中的方法名一致。
        resultType 表示查询结果要封装成 Employee 对象。
    -->
    <select id="selectByUsername" resultType="com.yunwei.pojo.entity.Employee">
        select id,
               name,
               username,
               password,
               phone,
               sex,
               id_number,
               status,
               create_time,
               update_time,
               create_user,
               update_user
        from employee
        where username = #{username}
    </select>


    <!-- 查询员工列表 -->
    <select id="selectAll"
            resultType="com.yunwei.pojo.entity.Employee">
        SELECT
            id,
            name,
            username,
            phone,
            sex,
            status,
            create_time,
            update_time
        FROM employee
        ORDER BY create_time DESC
    </select>


    <!-- 根据 ID 查询编辑弹窗需要的员工详情 -->
    <select id="selectById" resultType="com.yunwei.pojo.entity.Employee">
        SELECT id,
               username,
               name,
               phone,
               sex,
               id_number
        FROM employee
        WHERE id = #{id}
    </select>


    <!-- 修改员工基本信息，不修改密码和账号状态 -->
    <update id="update">
        UPDATE employee
        SET username = #{username},
        name = #{name},
        phone = #{phone},
        sex = #{sex},
        id_number = #{idNumber},
        update_time = #{updateTime},
        update_user = #{updateUser}
        WHERE id = #{id}
    </update>

</mapper>
